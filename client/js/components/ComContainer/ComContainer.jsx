import React from 'react';
import styles from './ComContainer.scss';

const ComContainer = function(props) {
    return (
        <div className={styles.comContainer}>
            test
        </div>
    );
};

export default ComContainer;
